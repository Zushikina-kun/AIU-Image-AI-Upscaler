# ─────────────────────────────────────────────────────────────────────────────
# AI Image Upscaler — Python runtime dependencies
# Install:  pip install -r requirements.txt
#
# Python 3.10+ required.
# Tested on Python 3.14 (system) and 3.10 (venv310).
#
# NOTE FOR BUILDING THE EXE:
#   Use a CLEAN venv with only these packages installed.
#   Do NOT install torch/scipy/sympy/numba — they bloat the EXE from ~60MB to 200+MB.
#   See README.md → Building from Source for exact steps.
# ─────────────────────────────────────────────────────────────────────────────

# ── Core GUI / image handling (always required) ───────────────────────────────
Pillow>=10.3.0

# ── Drag-and-drop support ─────────────────────────────────────────────────────
tkinterdnd2>=0.3.0

# ── Pre-processing: OpenCV NLM denoising ─────────────────────────────────────
# Headless build — no GUI window, ~80MB smaller than full opencv-python
opencv-python-headless>=4.9.0

# ── Background removal (rembg / U2Net) ───────────────────────────────────────
# Downloads U2Net model (~170MB) on first background-removal use
# Stored in ~/.u2net/ — not bundled in the EXE
rembg[cpu]>=2.0.50

# ── Subject detection + smart crop (YOLOv8n) ─────────────────────────────────
# Downloads yolov8n.pt (~6MB) on first smart-crop use
ultralytics>=8.0.0

# ── EXIF metadata preservation ────────────────────────────────────────────────
piexif>=1.1.3

# ─────────────────────────────────────────────────────────────────────────────
# Model Converter dependencies (only needed if you run "model converter.py")
# NOT installed by default — install manually if you need PTH→NCNN conversion
# ─────────────────────────────────────────────────────────────────────────────

# PyTorch — choose the right build for your hardware:
#   CPU only (works everywhere):
#     pip install torch==2.3.1 --index-url https://download.pytorch.org/whl/cpu
#   NVIDIA CUDA 12.1:
#     pip install torch==2.3.1 --index-url https://download.pytorch.org/whl/cu121

# Real-ESRGAN Python library (RealESRGANer + architecture defs)
# realesrgan>=0.3.0

# BasicSR — included as a git submodule (BasicSR/); no pip install needed

# safetensors — for .safetensors model weights
# safetensors>=0.4.3

# ONNX — intermediate format for PTH → NCNN pipeline
# onnx>=1.16.0
# onnxruntime>=1.18.0

# pnnx — modern PTH → NCNN converter (replaces onnx2ncnn)
# Download binary from: https://github.com/pnnx/pnnx/releases
# Place pnnx.exe in the project root or add to PATH
